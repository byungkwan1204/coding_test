package com.example.coding_test.이베이재팬;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Test;

public class FourthSolution {

    // SQL 문제

//    SELECT
//    p.ID as ID,
//    P.NAME as NAME,
//    SUM(s.PRICE) as 임대료
//    FROM SCHEDULES s
//    JOIN PLACES p ON p.ID = s.PLACE_ID
//    WHERE s.SCHEDULED_AT BETWEEN "2019-01-06 00:00:00" and "2019-01-07 00:00:00"
//    GROUP BY s.PLACE_ID
//    HAVING (COUNT(s.PLACE_ID) > 1 AND SUM(s.PRICE) <= 400);
}
